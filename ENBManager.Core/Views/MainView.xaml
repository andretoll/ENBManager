<UserControl x:Class="ENBManager.Core.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:loc="clr-namespace:ENBManager.Localization.Strings;assembly=ENBManager.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="25">
        <Viewbox MaxWidth="1200">
            <TextBlock Text="{x:Static loc:Strings.SELECT_A_GAME_IN_THE_MENU_TO_GET_STARTED}"
                       Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Viewbox>
    </Grid>
    
</UserControl>
